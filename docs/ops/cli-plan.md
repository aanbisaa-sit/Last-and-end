# CLI Plan

The command-line interface should provide three basic actions:

- load a config file
- run the configured checks
- print a short status summary

Storage and notification options can be added after the core loop is stable.
