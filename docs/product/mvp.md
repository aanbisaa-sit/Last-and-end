# MVP

The first useful version should provide endpoint configuration, check result modeling, status classification, summary rendering, and a small command-line path.

Non-goals for the MVP include user accounts, dashboards, hosted storage, and notification fan-out.
