[project]
name = "last-and-end"
version = "0.1.0"
description = "A lightweight uptime and downtime summary toolkit."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
